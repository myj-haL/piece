// import { useLazySubscription } from "./apolloHelper";

export enum EventUpdateTypes {
    Created = 'created',
    Deleted = 'deleted',
    Updated = 'updated',
}

export function useEventSubscription() {
    //TODO 여기가 뭐하는 곳인지 기억이 잘 안 나서 프로젝트 생성 중에 잠시 eventId 주석처리 함 나중에 처리 진행 예정
    // eventId
    return null;
}
